document.addEventListener("DOMContentLoaded", function () {
  const root = document.getElementById("root");
  if (!root) return;
  root.innerHTML = "<div style='font-family: sans-serif; padding: 1rem;'>✅ AG Grid Component is ready (but real AG Grid logic would be compiled here).</div>";
});
